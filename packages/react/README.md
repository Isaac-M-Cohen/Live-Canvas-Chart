# Live Canvas Chart React

```bash
npm install live-canvas-chart live-canvas-chart-react
```

```tsx
import { LiveCanvas } from "live-canvas-chart-react";

export function PriceChart({ data }) {
  return <LiveCanvas data={data} height={480} />;
}
```
